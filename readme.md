I would like to utilize the repo to generate and store a dataset for aircraft fault analysis. 
The type of aircraft should be a multi-role fighter using a single engine design.
- TODO: Document the design assumptions for the aircraft in a separate markdown. 
 - It should contain specify basic data of the system and its sub-systems


From the system/sub-system specification create datasets for the following information:
Dataset to list possible faults the aircraft can suffer from:
 - It should specify: long description of the fault, short description (max 20 letters), actions to rectify fault if possible 
Dataset to specify secondary effects of faults:
 - fault; derived/secondary faults
